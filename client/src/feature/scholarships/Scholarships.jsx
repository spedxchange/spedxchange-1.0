import React from 'react';
import { connect } from 'react-redux';

const mapState = state => {
  return {};
};

const actions = {};

const Scholarships = () => {
  return <h1>Scolarships</h1>;
};

export default connect(
  mapState,
  actions
)(Scholarships);
