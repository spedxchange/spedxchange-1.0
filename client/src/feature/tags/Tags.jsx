import React from 'react';
import { connect } from 'react-redux';

const mapState = state => {
  return {};
};

const actions = {};

const Tags = () => {
  return <h1>Tags</h1>;
};

export default connect(
  mapState,
  actions
)(Tags);
